import { Inter, Roboto_Mono, Rubik_Iso } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'], display: 'swap' })

export const roboto = Roboto_Mono({ subsets: ['latin'], display: 'swap', weight: "400" })

export const rubikIso = Rubik_Iso({ subsets: ['latin'], display: 'swap', weight: '400' })